import { Flex } from "native-base";
import { SettingsProps } from "./DefaultNavigator";

const SettingsPage: React.FC<SettingsProps> = ({ navigation, route }) => {
  return <Flex>Settings Page</Flex>;
};

export default SettingsPage;
